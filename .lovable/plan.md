
The user's logic check is correct: `12M avg/mo × 12 ≈ trailing-year total WIP movement`. If today's trailing-12-month total burn is ~$2.63M, then 12M avg should be ~$219K/mo. If the tile shows materially less, the calculation has a bug.

Let me inspect the current implementation to find the discrepancy.

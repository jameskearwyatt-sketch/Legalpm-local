# Netlify configuration for the Vite SPA.
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (e.g. /matters) don't 404 on refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
